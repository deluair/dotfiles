---
name: no-omtt-label
description: Never use "OMTT" label anywhere. The project is called BDPolicyLab (bdpolicylab.com).
type: feedback
---

Never use "OMTT" to refer to the bdpolicylab.com project. Always use "BDPolicyLab".

**Why:** User found it confusing. OMTT was an old internal name. The project, domain, repo, directory, and service are all "bdpolicylab" now.

**How to apply:**
- In all output, labels, tables, reports: use "BDPolicyLab"
- Repo is `deluair/bdpolicylab`, dir is `~/bdpolicylab`, domain is bdpolicylab.com
- VPS systemd service is `bdpolicylab`
- If you see "OMTT" anywhere in code/docs/memory, flag it for cleanup
