---
name: Always push everything — machineless workflow
description: User works across multiple machines; always commit and push all artifacts (plans, specs, docs) so nothing is machine-local only
type: feedback
---

Always ensure plans, specs, docs, and all project artifacts are committed and pushed to the remote repo — never leave them as local-only files. The user switches between machines and needs everything accessible via git clone/pull.
